@using PagedList.Mvc;
@model Bennington.Cms.Models.ListDetail.ListViewModel
@Html.PagedListPager(Model.Items, pageIndex => Url.Action("Index", new { pageIndex, sortby = Model.SortBy, sortDirection = Model.SortDirection, searchby = Model.SearchBy, searchvalue = Model.SearchValue }), Model.RenderOptions)